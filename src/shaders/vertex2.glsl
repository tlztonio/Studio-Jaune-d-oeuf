#include <beginnormal_vertex>

// float angle = sin( position.y * uMouse.x) * 0.2;
// mat2 rotateMatrix = get2dRotateMatrix(angle);
// float test2 = 1.0 - clamp(distance(objectNormal.xy,uMouse.xy), 0.0 , 1.0);

// objectNormal.xyz += clamp(distance(objectNormal.xyz,uMouse.xyz), 0.0 , 1.0);
// objectNormal.y += sin(objectNormal.y *5.0 + uTime) * 0.1;

// objectNormal.xy *= objectNormal.x*test2;